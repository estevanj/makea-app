import { StyleSheet } from "react-native";
import { FurnitureCard } from "../../AppStyles";

const styles = StyleSheet.create({
  container: FurnitureCard.container,
  photo: FurnitureCard.photo,
  title: FurnitureCard.title,
  category: FurnitureCard.category,
});

export default styles;
